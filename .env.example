# ==========================================
# Профспілка працівників освіти і науки
# Чернівецька міська організація
# ==========================================
# Vercel Deployment Configuration
# ==========================================
# These variables are set automatically when you add
# Neon Postgres and Blob Store through Vercel Dashboard.
# Only JWT_SECRET needs to be set manually.
# ==========================================

# Database (Neon Postgres - auto-set by Vercel Marketplace)
POSTGRES_URL="postgres://user:pass@host/db?sslmode=require"
POSTGRES_PRISMA_URL="postgres://user:pass@host/db?sslmode=require&pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgres://user:pass@host/db?sslmode=require"

# JWT Configuration
# SECURITY: Generate secure secret (min 256 bits / 32 bytes)
# Example: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_TO_SECURE_JWT_SECRET_MIN_256_BITS

# Vercel Blob Storage (auto-set by Vercel when Blob store is created)
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."
